### Arquivos

- Ler texto:
    - **FileReader**: se for ler um texto inteiro como fluxo de caracteres em blocos sem se importar com linhas pode usar o `FileReader.read(char[])`
    - **BufferedReader**: ele le um arquivo linha por linha com um buffer